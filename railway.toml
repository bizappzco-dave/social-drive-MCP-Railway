[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python3 simple_http_server.py"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
